# Start Here

1. Read `docs/foundations/REGION_DEFINITION.md`.
2. Read `docs/status/CANONICAL_MISSING_OBJECT.md`.
3. Run:
   `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q && lake build`
4. Read `docs/math/NEXT_THEOREM_OBJECT.md`.

Bounded outsider task:
- verify green build,
- locate the two canonical remaining objects,
- confirm status is conditional.
