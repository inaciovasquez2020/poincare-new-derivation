# Outsider Task

Task:
1. Run the full Python test suite.
2. Run `lake build`.
3. Confirm the repository states exactly two canonical remaining objects:
   - Local spherical descent lemma.
   - Zero-defect characterization: Phi(T)=0 => T ≃ S^3.
4. Confirm full closure remains conditional.

Pass condition:
- tests pass,
- build passes,
- the two remaining objects are found,
- conditional status is found.
